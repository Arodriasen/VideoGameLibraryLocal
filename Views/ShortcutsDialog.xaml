<Window x:Class="VideoGameLibrary.Views.ShortcutsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Atajos de teclado" Height="380" Width="420"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        FontFamily="{DynamicResource MaterialDesignFont}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Cabecera -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <StackPanel Orientation="Horizontal">
                <md:PackIcon Kind="KeyboardOutline" Width="24" Height="24" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <TextBlock Text="Atajos de teclado" FontSize="18" FontWeight="Medium" VerticalAlignment="Center"/>
            </StackPanel>
        </StackPanel>

        <!-- Lista -->
        <StackPanel Grid.Row="1">
            <Grid Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" Background="{DynamicResource MaterialDesignChipBackground}" CornerRadius="4"
                        Padding="8,4" HorizontalAlignment="Left" VerticalAlignment="Center">
                    <TextBlock Text="Ctrl + F" FontFamily="Consolas" FontSize="12"/>
                </Border>
                <TextBlock Grid.Column="1" Text="Enfocar el campo de búsqueda de la colección" VerticalAlignment="Center"
                           TextWrapping="Wrap" Margin="12,0,0,0"/>
            </Grid>

            <Grid Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" Background="{DynamicResource MaterialDesignChipBackground}" CornerRadius="4"
                        Padding="8,4" HorizontalAlignment="Left" VerticalAlignment="Center">
                    <TextBlock Text="Enter" FontFamily="Consolas" FontSize="12"/>
                </Border>
                <TextBlock Grid.Column="1" VerticalAlignment="Center" TextWrapping="Wrap" Margin="12,0,0,0"
                           Text="En la barra de escaneo rápido: busca el código y añade el juego a la colección automáticamente"/>
            </Grid>

            <Grid Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" Background="{DynamicResource MaterialDesignChipBackground}" CornerRadius="4"
                        Padding="8,4" HorizontalAlignment="Left" VerticalAlignment="Center">
                    <TextBlock Text="Enter" FontFamily="Consolas" FontSize="12"/>
                </Border>
                <TextBlock Grid.Column="1" VerticalAlignment="Center" TextWrapping="Wrap" Margin="12,0,0,0"
                           Text="En el campo de código de barras al añadir/editar un juego: busca su información"/>
            </Grid>

            <Grid Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" Background="{DynamicResource MaterialDesignChipBackground}" CornerRadius="4"
                        Padding="8,4" HorizontalAlignment="Left" VerticalAlignment="Center">
                    <TextBlock Text="Esc" FontFamily="Consolas" FontSize="12"/>
                </Border>
                <TextBlock Grid.Column="1" Text="Cierra el diálogo o ventana emergente actual" VerticalAlignment="Center"
                           TextWrapping="Wrap" Margin="12,0,0,0"/>
            </Grid>

            <TextBlock TextWrapping="Wrap" FontSize="12" Margin="0,4,0,0"
                       Foreground="{DynamicResource MaterialDesignBodyLight}"
                       Text="Consejo: un lector de códigos de barras USB funciona como un teclado, así que puedes escanear directamente sobre la barra de escaneo rápido o sobre el campo de código de barras."/>
        </StackPanel>

        <!-- Botones -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="CERRAR" Click="BtnClose_Click" IsCancel="True"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    md:ButtonAssist.CornerRadius="4"/>
        </StackPanel>
    </Grid>
</Window>
