<Window x:Class="VideoGameLibrary.Views.DuplicatesDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:conv="clr-namespace:VideoGameLibrary.Converters"
        Title="Buscar duplicados" Height="560" Width="480"
        MinHeight="400" MinWidth="420"
        WindowStartupLocation="CenterOwner" ResizeMode="CanResize"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        FontFamily="{DynamicResource MaterialDesignFont}">

    <Window.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVis"/>
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Cabecera -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <StackPanel Orientation="Horizontal">
                <md:PackIcon Kind="ContentDuplicate" Width="24" Height="24" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <TextBlock Text="Posibles duplicados" FontSize="18" FontWeight="Medium" VerticalAlignment="Center"/>
            </StackPanel>
            <TextBlock x:Name="TxtSummary" TextWrapping="Wrap" Margin="0,8,0,0" FontSize="12"
                       Foreground="{DynamicResource MaterialDesignBodyLight}"/>
            <TextBlock TextWrapping="Wrap" Margin="0,4,0,0" FontSize="12"
                       Foreground="{DynamicResource MaterialDesignBodyLight}"
                       Text="En cada grupo se preselecciona todo menos la copia más antigua. Lo que borres va a la papelera, así que puedes restaurarlo si te equivocas."/>
        </StackPanel>

        <!-- Lista -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="ItemsList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <TextBlock Text="{Binding GroupHeaderText}" FontSize="11" FontWeight="Medium"
                                       Foreground="{DynamicResource MaterialDesignBodyLight}"
                                       Margin="0,16,0,4"
                                       Visibility="{Binding IsGroupStart, Converter={StaticResource BoolToVis}}"/>
                            <Border Padding="8" Margin="0,0,0,4" BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="0,0,0,1">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <CheckBox Grid.Column="0" VerticalAlignment="Center" Margin="0,0,10,0"
                                              IsChecked="{Binding IsSelected}"/>

                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Title}" FontWeight="Medium" FontSize="13"/>
                                        <TextBlock FontSize="11">
                                            <Run Text="{Binding Platform, Mode=OneWay}" Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                                            <Run Text=" · " Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                                            <Run Text="{Binding BarcodeText, Mode=OneWay}" Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                                            <Run Text=" · " Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                                            <Run Text="{Binding AddedDateText, Mode=OneWay}" Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                                        </TextBlock>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Botones -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="CERRAR" Click="BtnCancel_Click" IsCancel="True"
                    Style="{StaticResource MaterialDesignOutlinedButton}" Margin="0,0,8,0"/>
            <Button Content="ENVIAR SELECCIONADOS A LA PAPELERA" Click="BtnDelete_Click"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    md:ButtonAssist.CornerRadius="4"/>
        </StackPanel>
    </Grid>
</Window>
