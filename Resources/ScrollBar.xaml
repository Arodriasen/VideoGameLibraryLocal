<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:sys="clr-namespace:System;assembly=mscorlib"
                    xmlns:wpf="clr-namespace:MaterialDesignThemes.Wpf;assembly=MaterialDesignThemes.Wpf">

    <!-- Scrollbar acorde al tema: mismo color que la cabecera (MaterialDesign.Brush.Primary, el
         que usan los md:ColorZone Mode="PrimaryMid". El alias antiguo "PrimaryHueMidBrush" NO
         está registrado en este proyecto porque su capa de compatibilidad no está mergeada en
         App.xaml; un DynamicResource a una clave inexistente no da error, simplemente no pinta
         nada; así se detectó este bug, probando primero con colores lisos), sin flechas y con
         el thumb fino y redondeado. Adaptado de MaterialDesignTheme.ScrollBar.xaml
         (MaterialDesignThemes 5.1.0), cambiando el color del thumb (el original usa
         MaterialDesign.Brush.Foreground) y el grosor. Sigue reaccionando al tema claro/oscuro
         porque MaterialDesign.Brush.Primary es un DynamicResource que PaletteHelper actualiza
         en caliente.

         IMPORTANTE (encontrado a base de probar): Track.Arrange le da al Thumb exactamente el
         ancho de la propia pista (ScrollBar.Width); pedirle al Border interior un Width mayor
         no sirve de nada si la pista en sí es estrecha, se recorta igual. Por eso aquí el
         ScrollBar tiene un ancho fijo (TrackThickness) generoso en vez de heredar
         SystemParameters.VerticalScrollBarWidthKey (que puede ser muy estrecho según el equipo),
         y el thumb crece dentro de ese ancho, nunca por encima. -->

    <sys:Double x:Key="AppScrollBarTrackThickness">16</sys:Double>
    <sys:Double x:Key="AppScrollBarThumbRestThickness">8</sys:Double>
    <sys:Double x:Key="AppScrollBarThumbHoverThickness">14</sys:Double>

    <Style x:Key="AppScrollBarThumb" TargetType="{x:Type Thumb}">
        <Setter Property="IsTabStop" Value="false" />
        <Setter Property="OverridesDefaultStyle" Value="true" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Thumb}">
                    <!-- Área de arrastre = todo el ancho de la pista (más fácil de agarrar);
                         dentro, el pill de color solo ocupa su grosor actual, centrado -->
                    <Border Background="Transparent">
                        <Border x:Name="border"
                                Width="{StaticResource AppScrollBarThumbRestThickness}"
                                Background="{DynamicResource MaterialDesign.Brush.Primary}"
                                CornerRadius="8"
                                Opacity="0.6"
                                SnapsToDevicePixels="True" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <DataTrigger Binding="{Binding Orientation, RelativeSource={RelativeSource FindAncestor, AncestorType=ScrollBar}}" Value="Horizontal">
                            <Setter TargetName="border" Property="Width" Value="Auto" />
                            <Setter TargetName="border" Property="Height" Value="{StaticResource AppScrollBarThumbRestThickness}" />
                        </DataTrigger>

                        <!-- Al pasar el ratón sobre TODA la barra (no solo sobre el thumb fino),
                             se hace más opaco y más grueso -->
                        <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource FindAncestor, AncestorType=ScrollBar}}" Value="True">
                            <Setter TargetName="border" Property="Opacity" Value="0.9" />
                        </DataTrigger>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding IsMouseOver, RelativeSource={RelativeSource FindAncestor, AncestorType=ScrollBar}}" Value="True" />
                                <Condition Binding="{Binding Orientation, RelativeSource={RelativeSource FindAncestor, AncestorType=ScrollBar}}" Value="Vertical" />
                            </MultiDataTrigger.Conditions>
                            <Setter TargetName="border" Property="Width" Value="{StaticResource AppScrollBarThumbHoverThickness}" />
                        </MultiDataTrigger>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding IsMouseOver, RelativeSource={RelativeSource FindAncestor, AncestorType=ScrollBar}}" Value="True" />
                                <Condition Binding="{Binding Orientation, RelativeSource={RelativeSource FindAncestor, AncestorType=ScrollBar}}" Value="Horizontal" />
                            </MultiDataTrigger.Conditions>
                            <Setter TargetName="border" Property="Height" Value="{StaticResource AppScrollBarThumbHoverThickness}" />
                        </MultiDataTrigger>

                        <Trigger Property="IsDragging" Value="true">
                            <Setter TargetName="border" Property="Opacity" Value="1" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Sin x:Key: reemplaza el estilo por defecto de ScrollBar en toda la app (declarado como
         último ResourceDictionary combinado en App.xaml, así que gana sobre el de MaterialDesignThemes) -->
    <Style TargetType="{x:Type ScrollBar}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Foreground" Value="{DynamicResource MaterialDesign.Brush.ScrollBar.Foreground}" />
        <Setter Property="MinWidth" Value="{StaticResource AppScrollBarTrackThickness}" />
        <Setter Property="Stylus.IsFlicksEnabled" Value="false" />
        <Setter Property="Stylus.IsPressAndHoldEnabled" Value="false" />
        <Setter Property="wpf:ScrollBarAssist.ButtonsVisibility" Value="Collapsed" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ScrollBar}">
                    <!-- Sin fundido propio: el único elemento visible es el thumb (pista y
                         botones son transparentes/colapsados), así que su opacidad/tamaño ya
                         los controla AppScrollBarThumb según el ratón esté encima o no -->
                    <Grid x:Name="Bg" SnapsToDevicePixels="true">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" MaxHeight="{DynamicResource {x:Static SystemParameters.VerticalScrollBarButtonHeightKey}}" />
                            <RowDefinition Height="0.00001*" />
                            <RowDefinition Height="Auto" MaxHeight="{DynamicResource {x:Static SystemParameters.VerticalScrollBarButtonHeightKey}}" />
                        </Grid.RowDefinitions>
                        <RepeatButton x:Name="PART_LineUpButton" Grid.Row="0" Command="{x:Static ScrollBar.LineUpCommand}"
                                      IsEnabled="{TemplateBinding IsMouseOver}" Style="{StaticResource MaterialDesignScrollBarButton}">
                            <Viewbox Width="12" Height="12" Margin="2,3,2,1">
                                <Canvas Width="24" Height="24">
                                    <Path Data="M13,20H11V8L5.5,13.5L4.08,12.08L12,4.16L19.92,12.08L18.5,13.5L13,8V20Z"
                                          Fill="{DynamicResource MaterialDesign.Brush.Foreground}" Stretch="Uniform" />
                                </Canvas>
                            </Viewbox>
                        </RepeatButton>
                        <Track x:Name="PART_Track" Grid.Row="1" IsDirectionReversed="true" IsEnabled="{TemplateBinding IsMouseOver}">
                            <Track.DecreaseRepeatButton>
                                <RepeatButton Command="{x:Static ScrollBar.PageUpCommand}" Style="{StaticResource MaterialDesignRepeatButtonTransparent}" />
                            </Track.DecreaseRepeatButton>
                            <Track.IncreaseRepeatButton>
                                <RepeatButton Command="{x:Static ScrollBar.PageDownCommand}" Style="{StaticResource MaterialDesignRepeatButtonTransparent}" />
                            </Track.IncreaseRepeatButton>
                            <Track.Thumb>
                                <Thumb Style="{StaticResource AppScrollBarThumb}" />
                            </Track.Thumb>
                        </Track>
                        <RepeatButton x:Name="PART_LineDownButton" Grid.Row="2" Command="{x:Static ScrollBar.LineDownCommand}"
                                      IsEnabled="{TemplateBinding IsMouseOver}" Style="{StaticResource MaterialDesignScrollBarButton}">
                            <Viewbox Width="12" Height="12" Margin="2,3,2,1">
                                <Canvas Width="24" Height="24">
                                    <Path Data="M11,4H13V16L18.5,10.5L19.92,11.92L12,19.84L4.08,11.92L5.5,10.5L11,16V4Z"
                                          Fill="{DynamicResource MaterialDesign.Brush.Foreground}" Stretch="Uniform" />
                                </Canvas>
                            </Viewbox>
                        </RepeatButton>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Width" Value="{StaticResource AppScrollBarTrackThickness}" />
        <Style.Triggers>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value=".56" />
            </Trigger>
            <Trigger Property="Orientation" Value="Horizontal">
                <Setter Property="Height" Value="{StaticResource AppScrollBarTrackThickness}" />
                <Setter Property="MinHeight" Value="{StaticResource AppScrollBarTrackThickness}" />
                <Setter Property="MinWidth" Value="0" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type ScrollBar}">
                            <Grid x:Name="Bg" SnapsToDevicePixels="true">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" MaxWidth="{DynamicResource {x:Static SystemParameters.HorizontalScrollBarButtonWidthKey}}" />
                                    <ColumnDefinition Width="0.00001*" />
                                    <ColumnDefinition Width="Auto" MaxWidth="{DynamicResource {x:Static SystemParameters.HorizontalScrollBarButtonWidthKey}}" />
                                </Grid.ColumnDefinitions>
                                <RepeatButton x:Name="PART_LineLeftButton" Grid.Column="0" Command="{x:Static ScrollBar.LineLeftCommand}"
                                              IsEnabled="{TemplateBinding IsMouseOver}" Style="{StaticResource MaterialDesignScrollBarButton}">
                                    <Viewbox Width="12" Height="12" Margin="3,2,1,2">
                                        <Canvas Width="24" Height="24">
                                            <Path Data="M20,11V13H8L13.5,18.5L12.08,19.92L4.16,12L12.08,4.08L13.5,5.5L8,11H20Z"
                                                  Fill="{DynamicResource MaterialDesign.Brush.Foreground}" Stretch="Uniform" />
                                        </Canvas>
                                    </Viewbox>
                                </RepeatButton>
                                <Track x:Name="PART_Track" Grid.Column="1" IsEnabled="{TemplateBinding IsMouseOver}">
                                    <Track.DecreaseRepeatButton>
                                        <RepeatButton Command="{x:Static ScrollBar.PageLeftCommand}" Style="{StaticResource MaterialDesignRepeatButtonTransparent}" />
                                    </Track.DecreaseRepeatButton>
                                    <Track.IncreaseRepeatButton>
                                        <RepeatButton Command="{x:Static ScrollBar.PageRightCommand}" Style="{StaticResource MaterialDesignRepeatButtonTransparent}" />
                                    </Track.IncreaseRepeatButton>
                                    <Track.Thumb>
                                        <Thumb Style="{StaticResource AppScrollBarThumb}" />
                                    </Track.Thumb>
                                </Track>
                                <RepeatButton x:Name="PART_LineRightButton" Grid.Column="2" Command="{x:Static ScrollBar.LineRightCommand}"
                                              IsEnabled="{TemplateBinding IsMouseOver}" Style="{StaticResource MaterialDesignScrollBarButton}">
                                    <Viewbox Width="12" Height="12" Margin="3,2,1,2">
                                        <Canvas Width="24" Height="24">
                                            <Path Data="M4,11V13H16L10.5,18.5L11.92,19.92L19.84,12L11.92,4.08L10.5,5.5L16,11H4Z"
                                                  Fill="{DynamicResource MaterialDesign.Brush.Foreground}" Stretch="Uniform" />
                                        </Canvas>
                                    </Viewbox>
                                </RepeatButton>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Setter Property="Width" Value="Auto" />
            </Trigger>
        </Style.Triggers>
    </Style>
</ResourceDictionary>
